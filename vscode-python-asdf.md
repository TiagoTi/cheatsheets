# Visual Code + Python + asdf + pipenv

```sh
#verifique se tem o dot-env e o  pipenv virtualenv e o virtualenvwrapper
cat ~/.default-python-packages

# ativando o ambiente virutal
pipenv shell

#
gem install bundle
bundle install
asdf reshim ruby 2.4.6
```
